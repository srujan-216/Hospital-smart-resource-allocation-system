# API
API_PORT=4000
WEB_ORIGIN=http://localhost:5173

# Auth (demo only — rotate in production)
JWT_SECRET=change-me-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=hackathon2026

# MongoDB (optional — leave blank to use in-memory store)
MONGODB_URI=

# AI (optional — leave blank to use rule-based fallback)
ANTHROPIC_API_KEY=
